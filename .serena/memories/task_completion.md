

### Commands to Run When a Task is Completed
- **Linting**: Use `flake8` or `mypy` to check for linting errors.
- **Formatting**: Use `black` to format the code.
- **Testing**: No testing framework is currently set up, but you can use `pytest` or `unittest` to write and run tests.

