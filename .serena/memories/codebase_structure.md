

- **`card.py`**: Defines the `Card` class with essential fields.
- **`scryfall/__init__.py`**: Implements the `ScryfallClient` for fetching cards from Scryfall.
- **`__init__.py`**: Exposes the `Card`, `ScryfallClient`, and `CardNotFoundError` for import.

