"""Integration tests for pymtg library.

This module contains integration tests that test the library against real API
endpoints. These tests require network access and may require API credentials.

Tests in this module are designed to be skipped if:
- Network access is not available
- Required API credentials are not configured
- Rate limits would be exceeded
"""
